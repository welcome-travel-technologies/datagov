# Shell scripts must use LF so they execute inside Linux containers.
# On Windows, core.autocrlf=true would otherwise rewrite them to CRLF on checkout,
# corrupting the shebang line and causing: exec /entrypoint.sh: no such file or directory
*.sh text eol=lf
entrypoint.sh text eol=lf
