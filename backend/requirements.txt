# Canonical dependencies live beside the Django application because that is
# the file copied by backend/Dockerfile. Keep this forwarding file so existing
# local/CI commands (`pip install -r backend/requirements.txt`) continue to
# work without maintaining a second, drifting dependency list.
-r app/requirements.txt
